# Create an app at https://developer.spotify.com/dashboard, then paste its
# Client ID / Secret here and rename this file to spin.env.
# Search only needs client credentials - no login, no redirect URI, no scopes.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
